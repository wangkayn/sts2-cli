// Global using aliases to resolve type ambiguities between Godot and System namespaces.
// These are only needed for specific decompiled files, so we suppress with aliases.

// Note: The actual fix for ambiguities is to exclude the problematic files since they're
// not needed for headless combat simulation. But we can't easily do that with global aliases.
// Instead, we'll exclude the specific problematic files from compilation.
